{
  "name": "cph-my-stay",
  "version": "1.0.0",
  "private": true,
  "description": "Caribbean Paradise Homes — My Stay guest app (backend + frontend), wired to the 365Villas/GuestWisely PMS. Zero npm dependencies — pure Node stdlib.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "V365_MOCK=1 SESSION_SECRET=dev-secret-change-me NODE_ENV=development node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
